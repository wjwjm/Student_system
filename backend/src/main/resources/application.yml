spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_system?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  jwt:
    secret: offline-local-signing-key-please-change-to-32-plus-bytes
    expiration-ms: 28800000
